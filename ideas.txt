Ideas we're considering
+++++++++++++++++++++++

# builder

Docstrings in html subpackage.

Documentation

# Mako lat/lon examples
# Update README.txt
# Change README to README.txt in MANIFEST.in


Postponing for further consideration
====================================

size callback for image()?   size(path) => (width, height) or (None, None).

# tags

class Select(object):
    .option(value, content=None) => if no content, set content=value and
        value=NotGiven)
    .group(content) => create group and return it so user can populate it
    .extend(items) => same as calling option several times

Rewrite 'select()'. 
Allow options to be list of strings; generate simple options without 'value'.
Simplify Options and Select.


Disfavoring
===========

javascript_link -> js? (conflict with js library)
stylesheet_link -> css? (user can rename on import)
